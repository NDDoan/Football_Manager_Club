<Window x:Class="FootballManagerWPF.Views.AdminNotificationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManagerWPF.Views"
        mc:Ignorable="d"
        Title="Quản lý Thông báo" Height="400" Width="600"
        WindowStartupLocation="CenterScreen">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <StackPanel>
                <TextBlock Text="Gửi Thông báo nội bộ" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

                <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                    <TextBlock Text="Tiêu đề:"/>
                    <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" Width="500"/>
                    <TextBlock Text="Nội dung:" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding NewContent, UpdateSourceTrigger=PropertyChanged}" Width="500" Height="80" AcceptsReturn="True"/>
                    <TextBlock Text="Phòng ban (để trống nếu gửi cho tất cả):" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding TargetDepartmentId, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                    <Button Content="Gửi thông báo" Command="{Binding SendNotificationCommand}" Width="150" Margin="0,10,0,0"/>
                </StackPanel>

                <TextBlock Text="Danh sách Thông báo đã gửi" FontSize="20" FontWeight="Bold" Margin="0,10,0,10"/>
                <DataGrid ItemsSource="{Binding Notifications}" AutoGenerateColumns="False" Height="150">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding NotificationId}" Width="Auto"/>
                        <DataGridTextColumn Header="Tiêu đề" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Nội dung" Binding="{Binding Content}" Width="2*"/>
                        <DataGridTextColumn Header="Ngày gửi" Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Content="Làm mới" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" Width="100" Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
