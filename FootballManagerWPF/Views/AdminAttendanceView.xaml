<Window x:Class="FootballManagerWPF.Views.AdminAttendanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManagerWPF.Views"
        mc:Ignorable="d"
        Title="Quản lý chấm công" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Danh sách chấm công của tất cả nhân viên" 
                        FontSize="20" FontWeight="Bold" Margin="0,0,0,10" 
                        HorizontalAlignment="Center"/>
            <DataGrid ItemsSource="{Binding AttendanceRecords}" 
                       AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Attendance ID" 
                                         Binding="{Binding AttendanceId}" 
                                         Width="Auto"/>
                    <DataGridTextColumn Header="Employee ID" 
                                         Binding="{Binding EmployeeId}" 
                                         Width="Auto"/>
                    <DataGridTextColumn Header="Date" 
                                         Binding="{Binding AttendanceDate, StringFormat='dd/MM/yyyy'}" 
                                         Width="*"/>
                    <DataGridTextColumn Header="Check In" 
                                         Binding="{Binding CheckInTime, StringFormat='HH:mm:ss'}" 
                                         Width="*"/>
                    <DataGridTextColumn Header="Check Out" 
                                         Binding="{Binding CheckOutTime, StringFormat='HH:mm:ss'}" 
                                         Width="*"/>
                    <DataGridTextColumn Header="Overtime" 
                                         Binding="{Binding OvertimeHours}" 
                                         Width="Auto"/>
                    <DataGridTextColumn Header="Leave Type" 
                                         Binding="{Binding LeaveType}" 
                                         Width="Auto"/>
                    <DataGridTextColumn Header="Created At" 
                                         Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm:ss'}" 
                                         Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Làm mới" 
                     Command="{Binding RefreshCommand}" 
                     HorizontalAlignment="Right" 
                     Width="100" 
                     Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
