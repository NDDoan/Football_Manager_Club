<Window x:Class="FootballManagerWPF.Views.EmployeePayrollView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManagerWPF.Views"
        mc:Ignorable="d"
        Title="Bảng lương của bạn" Height="400" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Bảng lương của bạn" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding PayrollRecords}" AutoGenerateColumns="False" Height="250">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Basic Salary" Binding="{Binding BaseSalary}" Width="*"/>
                    <DataGridTextColumn Header="Allowance" Binding="{Binding Allowance}" Width="*"/>
                    <DataGridTextColumn Header="Bonus" Binding="{Binding Bonus}" Width="*"/>
                    <DataGridTextColumn Header="Penalty" Binding="{Binding Penalty}" Width="*"/>
                    <DataGridTextColumn Header="Total Income" Binding="{Binding TotalIncome}" Width="*"/>
                    <DataGridTextColumn Header="Payroll Date" Binding="{Binding PayrollDate, StringFormat='dd/MM/yyyy'}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Làm mới" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" Width="100" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
