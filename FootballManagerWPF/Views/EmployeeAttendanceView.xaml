<Window x:Class="FootballManagerWPF.Views.EmployeeAttendanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManagerWPF.Views"
        mc:Ignorable="d"
        Title="Chấm công của bạn" Height="400" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Lịch chấm công của bạn" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding AttendanceRecords}" 
                      AutoGenerateColumns="False" Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AttendanceId}" Width="Auto"/>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding AttendanceDate, StringFormat='dd/MM/yyyy'}" Width="*"/>
                    <DataGridTextColumn Header="Check In" Binding="{Binding CheckInTime, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutTime, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Overtime" Binding="{Binding OvertimeHours}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Làm mới" Command="{Binding RefreshAttendanceCommand}" Width="100" Margin="5"/>
                <Button Content="Check In" Command="{Binding ClockInCommand}" Width="100" Margin="5"/>
                <Button Content="Check Out" Command="{Binding ClockOutCommand}" Width="100" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
