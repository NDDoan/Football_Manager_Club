<Window x:Class="FootballManagerWPF.Views.ActivityLogManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FootballManagerWPF.Views"
        mc:Ignorable="d"
        Title="Activity Log Management" Height="400" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Quản lý Lịch sử Hoạt động" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding ActivityLogs}" AutoGenerateColumns="False" Height="250">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding LogId}" Width="Auto"/>
                    <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="*"/>
                    <DataGridTextColumn Header="Activity" Binding="{Binding Activity}" Width="*"/>
                    <DataGridTextColumn Header="Table" Binding="{Binding TableName}" Width="*"/>
                    <DataGridTextColumn Header="Record ID" Binding="{Binding RecordId}" Width="*"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="2*"/>
                    <DataGridTextColumn Header="Timestamp" Binding="{Binding ActivityTimestamp}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Làm mới" Command="{Binding RefreshActivityLogCommand}" 
                    HorizontalAlignment="Right" Margin="0,10,0,0" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
